rule "Update Configuration"
when
    updateGit changed from OFF to ON
then
	executeCommandLine("cd /etc/openhab2 && git fetch --all && git reset --hard origin/master && git pull origin master && chmod -R 777 /etc/openhab2")
	updateGit.sendCommand(OFF)
end

